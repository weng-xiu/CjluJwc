# 系统架构设计

# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
># 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 REST# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端 API 服务于多个前端入口（管理端、门户端、未来可能的移动 App 等），最大化# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端 API 服务于多个前端入口（管理端、门户端、未来可能的移动 App 等），最大化接口复用率。

**模块化设计原则**：后端采用 Maven 多模块架构，# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端 API 服务于多个前端入口（管理端、门户端、未来可能的移动 App 等），最大化接口复用率。

**模块化设计原则**：后端采用 Maven 多模块架构，将系统拆分为 13 个功能模块，每个模块承担明确的业务职责。模块之间通过# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端 API 服务于多个前端入口（管理端、门户端、未来可能的移动 App 等），最大化接口复用率。

**模块化设计原则**：后端采用 Maven 多模块架构，将系统拆分为 13 个功能模块，每个模块承担明确的业务职责。模块之间通过依赖注入和接口抽象实现松耦合，遵循# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端 API 服务于多个前端入口（管理端、门户端、未来可能的移动 App 等），最大化接口复用率。

**模块化设计原则**：后端采用 Maven 多模块架构，将系统拆分为 13 个功能模块，每个模块承担明确的业务职责。模块之间通过依赖注入和接口抽象实现松耦合，遵循"高内聚、低耦合"的设计哲学。新增业务功能时只需创建新的业务模块# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端 API 服务于多个前端入口（管理端、门户端、未来可能的移动 App 等），最大化接口复用率。

**模块化设计原则**：后端采用 Maven 多模块架构，将系统拆分为 13 个功能模块，每个模块承担明确的业务职责。模块之间通过依赖注入和接口抽象实现松耦合，遵循"高内聚、低耦合"的设计哲学。新增业务功能时只需创建新的业务模块，不影响已有模块的稳定性。

**分层架构原则**：在每个模块内部，严格遵循表现# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端 API 服务于多个前端入口（管理端、门户端、未来可能的移动 App 等），最大化接口复用率。

**模块化设计原则**：后端采用 Maven 多模块架构，将系统拆分为 13 个功能模块，每个模块承担明确的业务职责。模块之间通过依赖注入和接口抽象实现松耦合，遵循"高内聚、低耦合"的设计哲学。新增业务功能时只需创建新的业务模块，不影响已有模块的稳定性。

**分层架构原则**：在每个模块内部，严格遵循表现层（Controller）、业务层（Service）、持久层（Mapper）的三层分离模式。表现# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端 API 服务于多个前端入口（管理端、门户端、未来可能的移动 App 等），最大化接口复用率。

**模块化设计原则**：后端采用 Maven 多模块架构，将系统拆分为 13 个功能模块，每个模块承担明确的业务职责。模块之间通过依赖注入和接口抽象实现松耦合，遵循"高内聚、低耦合"的设计哲学。新增业务功能时只需创建新的业务模块，不影响已有模块的稳定性。

**分层架构原则**：在每个模块内部，严格遵循表现层（Controller）、业务层（Service）、持久层（Mapper）的三层分离模式。表现层负责协议处理和参数校验，业务层负责核心业务逻辑和事务管理，持久# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端 API 服务于多个前端入口（管理端、门户端、未来可能的移动 App 等），最大化接口复用率。

**模块化设计原则**：后端采用 Maven 多模块架构，将系统拆分为 13 个功能模块，每个模块承担明确的业务职责。模块之间通过依赖注入和接口抽象实现松耦合，遵循"高内聚、低耦合"的设计哲学。新增业务功能时只需创建新的业务模块，不影响已有模块的稳定性。

**分层架构原则**：在每个模块内部，严格遵循表现层（Controller）、业务层（Service）、持久层（Mapper）的三层分离模式。表现层负责协议处理和参数校验，业务层负责核心业务逻辑和事务管理，持久层负责数据访问。基础设施层（yu-framework# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端 API 服务于多个前端入口（管理端、门户端、未来可能的移动 App 等），最大化接口复用率。

**模块化设计原则**：后端采用 Maven 多模块架构，将系统拆分为 13 个功能模块，每个模块承担明确的业务职责。模块之间通过依赖注入和接口抽象实现松耦合，遵循"高内聚、低耦合"的设计哲学。新增业务功能时只需创建新的业务模块，不影响已有模块的稳定性。

**分层架构原则**：在每个模块内部，严格遵循表现层（Controller）、业务层（Service）、持久层（Mapper）的三层分离模式。表现层负责协议处理和参数校验，业务层负责核心业务逻辑和事务管理，持久层负责数据访问。基础设施层（yu-framework）提供横切关注点的统一处理，包括# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端 API 服务于多个前端入口（管理端、门户端、未来可能的移动 App 等），最大化接口复用率。

**模块化设计原则**：后端采用 Maven 多模块架构，将系统拆分为 13 个功能模块，每个模块承担明确的业务职责。模块之间通过依赖注入和接口抽象实现松耦合，遵循"高内聚、低耦合"的设计哲学。新增业务功能时只需创建新的业务模块，不影响已有模块的稳定性。

**分层架构原则**：在每个模块内部，严格遵循表现层（Controller）、业务层（Service）、持久层（Mapper）的三层分离模式。表现层负责协议处理和参数校验，业务层负责核心业务逻辑和事务管理，持久层负责数据访问。基础设施层（yu-framework）提供横切关注点的统一处理，包括安全认证、缓存管理、数据源切换、# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端 API 服务于多个前端入口（管理端、门户端、未来可能的移动 App 等），最大化接口复用率。

**模块化设计原则**：后端采用 Maven 多模块架构，将系统拆分为 13 个功能模块，每个模块承担明确的业务职责。模块之间通过依赖注入和接口抽象实现松耦合，遵循"高内聚、低耦合"的设计哲学。新增业务功能时只需创建新的业务模块，不影响已有模块的稳定性。

**分层架构原则**：在每个模块内部，严格遵循表现层（Controller）、业务层（Service）、持久层（Mapper）的三层分离模式。表现层负责协议处理和参数校验，业务层负责核心业务逻辑和事务管理，持久层负责数据访问。基础设施层（yu-framework）提供横切关注点的统一处理，包括安全认证、缓存管理、数据源切换、日志记录等。

**可扩展性原则**：系统在设计时充分考虑了未来业务增长的需要# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端 API 服务于多个前端入口（管理端、门户端、未来可能的移动 App 等），最大化接口复用率。

**模块化设计原则**：后端采用 Maven 多模块架构，将系统拆分为 13 个功能模块，每个模块承担明确的业务职责。模块之间通过依赖注入和接口抽象实现松耦合，遵循"高内聚、低耦合"的设计哲学。新增业务功能时只需创建新的业务模块，不影响已有模块的稳定性。

**分层架构原则**：在每个模块内部，严格遵循表现层（Controller）、业务层（Service）、持久层（Mapper）的三层分离模式。表现层负责协议处理和参数校验，业务层负责核心业务逻辑和事务管理，持久层负责数据访问。基础设施层（yu-framework）提供横切关注点的统一处理，包括安全认证、缓存管理、数据源切换、日志记录等。

**可扩展性原则**：系统在设计时充分考虑了未来业务增长的需要。数据层支持读写分离和多数据源切换# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端 API 服务于多个前端入口（管理端、门户端、未来可能的移动 App 等），最大化接口复用率。

**模块化设计原则**：后端采用 Maven 多模块架构，将系统拆分为 13 个功能模块，每个模块承担明确的业务职责。模块之间通过依赖注入和接口抽象实现松耦合，遵循"高内聚、低耦合"的设计哲学。新增业务功能时只需创建新的业务模块，不影响已有模块的稳定性。

**分层架构原则**：在每个模块内部，严格遵循表现层（Controller）、业务层（Service）、持久层（Mapper）的三层分离模式。表现层负责协议处理和参数校验，业务层负责核心业务逻辑和事务管理，持久层负责数据访问。基础设施层（yu-framework）提供横切关注点的统一处理，包括安全认证、缓存管理、数据源切换、日志记录等。

**可扩展性原则**：系统在设计时充分考虑了未来业务增长的需要。数据层支持读写分离和多数据源切换，缓存层采用 Redis 提供高性能数据访问，调度层采用 Quartz 支持定时# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端 API 服务于多个前端入口（管理端、门户端、未来可能的移动 App 等），最大化接口复用率。

**模块化设计原则**：后端采用 Maven 多模块架构，将系统拆分为 13 个功能模块，每个模块承担明确的业务职责。模块之间通过依赖注入和接口抽象实现松耦合，遵循"高内聚、低耦合"的设计哲学。新增业务功能时只需创建新的业务模块，不影响已有模块的稳定性。

**分层架构原则**：在每个模块内部，严格遵循表现层（Controller）、业务层（Service）、持久层（Mapper）的三层分离模式。表现层负责协议处理和参数校验，业务层负责核心业务逻辑和事务管理，持久层负责数据访问。基础设施层（yu-framework）提供横切关注点的统一处理，包括安全认证、缓存管理、数据源切换、日志记录等。

**可扩展性原则**：系统在设计时充分考虑了未来业务增长的需要。数据层支持读写分离和多数据源切换，缓存层采用 Redis 提供高性能数据访问，调度层采用 Quartz 支持定时任务的动态增减，工作流层采用 Flow# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端 API 服务于多个前端入口（管理端、门户端、未来可能的移动 App 等），最大化接口复用率。

**模块化设计原则**：后端采用 Maven 多模块架构，将系统拆分为 13 个功能模块，每个模块承担明确的业务职责。模块之间通过依赖注入和接口抽象实现松耦合，遵循"高内聚、低耦合"的设计哲学。新增业务功能时只需创建新的业务模块，不影响已有模块的稳定性。

**分层架构原则**：在每个模块内部，严格遵循表现层（Controller）、业务层（Service）、持久层（Mapper）的三层分离模式。表现层负责协议处理和参数校验，业务层负责核心业务逻辑和事务管理，持久层负责数据访问。基础设施层（yu-framework）提供横切关注点的统一处理，包括安全认证、缓存管理、数据源切换、日志记录等。

**可扩展性原则**：系统在设计时充分考虑了未来业务增长的需要。数据层支持读写分离和多数据源切换，缓存层采用 Redis 提供高性能数据访问，调度层采用 Quartz 支持定时任务的动态增减，工作流层采用 Flowable 支持审批流程的灵活定制。# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端 API 服务于多个前端入口（管理端、门户端、未来可能的移动 App 等），最大化接口复用率。

**模块化设计原则**：后端采用 Maven 多模块架构，将系统拆分为 13 个功能模块，每个模块承担明确的业务职责。模块之间通过依赖注入和接口抽象实现松耦合，遵循"高内聚、低耦合"的设计哲学。新增业务功能时只需创建新的业务模块，不影响已有模块的稳定性。

**分层架构原则**：在每个模块内部，严格遵循表现层（Controller）、业务层（Service）、持久层（Mapper）的三层分离模式。表现层负责协议处理和参数校验，业务层负责核心业务逻辑和事务管理，持久层负责数据访问。基础设施层（yu-framework）提供横切关注点的统一处理，包括安全认证、缓存管理、数据源切换、日志记录等。

**可扩展性原则**：系统在设计时充分考虑了未来业务增长的需要。数据层支持读写分离和多数据源切换，缓存层采用 Redis 提供高性能数据访问，调度层采用 Quartz 支持定时任务的动态增减，工作流层采用 Flowable 支持审批流程的灵活定制。前端路由采用动态加载机制，菜单和权限可由后台配置实时调整。

### 1# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端 API 服务于多个前端入口（管理端、门户端、未来可能的移动 App 等），最大化接口复用率。

**模块化设计原则**：后端采用 Maven 多模块架构，将系统拆分为 13 个功能模块，每个模块承担明确的业务职责。模块之间通过依赖注入和接口抽象实现松耦合，遵循"高内聚、低耦合"的设计哲学。新增业务功能时只需创建新的业务模块，不影响已有模块的稳定性。

**分层架构原则**：在每个模块内部，严格遵循表现层（Controller）、业务层（Service）、持久层（Mapper）的三层分离模式。表现层负责协议处理和参数校验，业务层负责核心业务逻辑和事务管理，持久层负责数据访问。基础设施层（yu-framework）提供横切关注点的统一处理，包括安全认证、缓存管理、数据源切换、日志记录等。

**可扩展性原则**：系统在设计时充分考虑了未来业务增长的需要。数据层支持读写分离和多数据源切换，缓存层采用 Redis 提供高性能数据访问，调度层采用 Quartz 支持定时任务的动态增减，工作流层采用 Flowable 支持审批流程的灵活定制。前端路由采用动态加载机制，菜单和权限可由后台配置实时调整。

### 1.2 总体架构说明

系统整体采用# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端 API 服务于多个前端入口（管理端、门户端、未来可能的移动 App 等），最大化接口复用率。

**模块化设计原则**：后端采用 Maven 多模块架构，将系统拆分为 13 个功能模块，每个模块承担明确的业务职责。模块之间通过依赖注入和接口抽象实现松耦合，遵循"高内聚、低耦合"的设计哲学。新增业务功能时只需创建新的业务模块，不影响已有模块的稳定性。

**分层架构原则**：在每个模块内部，严格遵循表现层（Controller）、业务层（Service）、持久层（Mapper）的三层分离模式。表现层负责协议处理和参数校验，业务层负责核心业务逻辑和事务管理，持久层负责数据访问。基础设施层（yu-framework）提供横切关注点的统一处理，包括安全认证、缓存管理、数据源切换、日志记录等。

**可扩展性原则**：系统在设计时充分考虑了未来业务增长的需要。数据层支持读写分离和多数据源切换，缓存层采用 Redis 提供高性能数据访问，调度层采用 Quartz 支持定时任务的动态增减，工作流层采用 Flowable 支持审批流程的灵活定制。前端路由采用动态加载机制，菜单和权限可由后台配置实时调整。

### 1.2 总体架构说明

系统整体采用前后端分离的双端架构，由以下几个核心部分组成：

- **后台管理端（yu# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端 API 服务于多个前端入口（管理端、门户端、未来可能的移动 App 等），最大化接口复用率。

**模块化设计原则**：后端采用 Maven 多模块架构，将系统拆分为 13 个功能模块，每个模块承担明确的业务职责。模块之间通过依赖注入和接口抽象实现松耦合，遵循"高内聚、低耦合"的设计哲学。新增业务功能时只需创建新的业务模块，不影响已有模块的稳定性。

**分层架构原则**：在每个模块内部，严格遵循表现层（Controller）、业务层（Service）、持久层（Mapper）的三层分离模式。表现层负责协议处理和参数校验，业务层负责核心业务逻辑和事务管理，持久层负责数据访问。基础设施层（yu-framework）提供横切关注点的统一处理，包括安全认证、缓存管理、数据源切换、日志记录等。

**可扩展性原则**：系统在设计时充分考虑了未来业务增长的需要。数据层支持读写分离和多数据源切换，缓存层采用 Redis 提供高性能数据访问，调度层采用 Quartz 支持定时任务的动态增减，工作流层采用 Flowable 支持审批流程的灵活定制。前端路由采用动态加载机制，菜单和权限可由后台配置实时调整。

### 1.2 总体架构说明

系统整体采用前后端分离的双端架构，由以下几个核心部分组成：

- **后台管理端（yu-ui）**：基于 Vue2 + ElementUI 技术栈构建，面向教务管理人员提供# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端 API 服务于多个前端入口（管理端、门户端、未来可能的移动 App 等），最大化接口复用率。

**模块化设计原则**：后端采用 Maven 多模块架构，将系统拆分为 13 个功能模块，每个模块承担明确的业务职责。模块之间通过依赖注入和接口抽象实现松耦合，遵循"高内聚、低耦合"的设计哲学。新增业务功能时只需创建新的业务模块，不影响已有模块的稳定性。

**分层架构原则**：在每个模块内部，严格遵循表现层（Controller）、业务层（Service）、持久层（Mapper）的三层分离模式。表现层负责协议处理和参数校验，业务层负责核心业务逻辑和事务管理，持久层负责数据访问。基础设施层（yu-framework）提供横切关注点的统一处理，包括安全认证、缓存管理、数据源切换、日志记录等。

**可扩展性原则**：系统在设计时充分考虑了未来业务增长的需要。数据层支持读写分离和多数据源切换，缓存层采用 Redis 提供高性能数据访问，调度层采用 Quartz 支持定时任务的动态增减，工作流层采用 Flowable 支持审批流程的灵活定制。前端路由采用动态加载机制，菜单和权限可由后台配置实时调整。

### 1.2 总体架构说明

系统整体采用前后端分离的双端架构，由以下几个核心部分组成：

- **后台管理端（yu-ui）**：基于 Vue2 + ElementUI 技术栈构建，面向教务管理人员提供全面的教务管理功能。包含学生管理、教师# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端 API 服务于多个前端入口（管理端、门户端、未来可能的移动 App 等），最大化接口复用率。

**模块化设计原则**：后端采用 Maven 多模块架构，将系统拆分为 13 个功能模块，每个模块承担明确的业务职责。模块之间通过依赖注入和接口抽象实现松耦合，遵循"高内聚、低耦合"的设计哲学。新增业务功能时只需创建新的业务模块，不影响已有模块的稳定性。

**分层架构原则**：在每个模块内部，严格遵循表现层（Controller）、业务层（Service）、持久层（Mapper）的三层分离模式。表现层负责协议处理和参数校验，业务层负责核心业务逻辑和事务管理，持久层负责数据访问。基础设施层（yu-framework）提供横切关注点的统一处理，包括安全认证、缓存管理、数据源切换、日志记录等。

**可扩展性原则**：系统在设计时充分考虑了未来业务增长的需要。数据层支持读写分离和多数据源切换，缓存层采用 Redis 提供高性能数据访问，调度层采用 Quartz 支持定时任务的动态增减，工作流层采用 Flowable 支持审批流程的灵活定制。前端路由采用动态加载机制，菜单和权限可由后台配置实时调整。

### 1.2 总体架构说明

系统整体采用前后端分离的双端架构，由以下几个核心部分组成：

- **后台管理端（yu-ui）**：基于 Vue2 + ElementUI 技术栈构建，面向教务管理人员提供全面的教务管理功能。包含学生管理、教师管理、课程管理、选课管理、成绩管理、排课管理、OA 审批# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端 API 服务于多个前端入口（管理端、门户端、未来可能的移动 App 等），最大化接口复用率。

**模块化设计原则**：后端采用 Maven 多模块架构，将系统拆分为 13 个功能模块，每个模块承担明确的业务职责。模块之间通过依赖注入和接口抽象实现松耦合，遵循"高内聚、低耦合"的设计哲学。新增业务功能时只需创建新的业务模块，不影响已有模块的稳定性。

**分层架构原则**：在每个模块内部，严格遵循表现层（Controller）、业务层（Service）、持久层（Mapper）的三层分离模式。表现层负责协议处理和参数校验，业务层负责核心业务逻辑和事务管理，持久层负责数据访问。基础设施层（yu-framework）提供横切关注点的统一处理，包括安全认证、缓存管理、数据源切换、日志记录等。

**可扩展性原则**：系统在设计时充分考虑了未来业务增长的需要。数据层支持读写分离和多数据源切换，缓存层采用 Redis 提供高性能数据访问，调度层采用 Quartz 支持定时任务的动态增减，工作流层采用 Flowable 支持审批流程的灵活定制。前端路由采用动态加载机制，菜单和权限可由后台配置实时调整。

### 1.2 总体架构说明

系统整体采用前后端分离的双端架构，由以下几个核心部分组成：

- **后台管理端（yu-ui）**：基于 Vue2 + ElementUI 技术栈构建，面向教务管理人员提供全面的教务管理功能。包含学生管理、教师管理、课程管理、选课管理、成绩管理、排课管理、OA 审批等核心业务模块。
- **师生门户端（yu-portal-ui）**：基于轻量级 Vue2 构建，面向教师和学生提供自助服务功能。支持 PC 端和移动# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端 API 服务于多个前端入口（管理端、门户端、未来可能的移动 App 等），最大化接口复用率。

**模块化设计原则**：后端采用 Maven 多模块架构，将系统拆分为 13 个功能模块，每个模块承担明确的业务职责。模块之间通过依赖注入和接口抽象实现松耦合，遵循"高内聚、低耦合"的设计哲学。新增业务功能时只需创建新的业务模块，不影响已有模块的稳定性。

**分层架构原则**：在每个模块内部，严格遵循表现层（Controller）、业务层（Service）、持久层（Mapper）的三层分离模式。表现层负责协议处理和参数校验，业务层负责核心业务逻辑和事务管理，持久层负责数据访问。基础设施层（yu-framework）提供横切关注点的统一处理，包括安全认证、缓存管理、数据源切换、日志记录等。

**可扩展性原则**：系统在设计时充分考虑了未来业务增长的需要。数据层支持读写分离和多数据源切换，缓存层采用 Redis 提供高性能数据访问，调度层采用 Quartz 支持定时任务的动态增减，工作流层采用 Flowable 支持审批流程的灵活定制。前端路由采用动态加载机制，菜单和权限可由后台配置实时调整。

### 1.2 总体架构说明

系统整体采用前后端分离的双端架构，由以下几个核心部分组成：

- **后台管理端（yu-ui）**：基于 Vue2 + ElementUI 技术栈构建，面向教务管理人员提供全面的教务管理功能。包含学生管理、教师管理、课程管理、选课管理、成绩管理、排课管理、OA 审批等核心业务模块。
- **师生门户端（yu-portal-ui）**：基于轻量级 Vue2 构建，面向教师和学生提供自助服务功能。支持 PC 端和移动端双端访问，提供成绩查询、选课操作、课表查看、通知公告等功能# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端 API 服务于多个前端入口（管理端、门户端、未来可能的移动 App 等），最大化接口复用率。

**模块化设计原则**：后端采用 Maven 多模块架构，将系统拆分为 13 个功能模块，每个模块承担明确的业务职责。模块之间通过依赖注入和接口抽象实现松耦合，遵循"高内聚、低耦合"的设计哲学。新增业务功能时只需创建新的业务模块，不影响已有模块的稳定性。

**分层架构原则**：在每个模块内部，严格遵循表现层（Controller）、业务层（Service）、持久层（Mapper）的三层分离模式。表现层负责协议处理和参数校验，业务层负责核心业务逻辑和事务管理，持久层负责数据访问。基础设施层（yu-framework）提供横切关注点的统一处理，包括安全认证、缓存管理、数据源切换、日志记录等。

**可扩展性原则**：系统在设计时充分考虑了未来业务增长的需要。数据层支持读写分离和多数据源切换，缓存层采用 Redis 提供高性能数据访问，调度层采用 Quartz 支持定时任务的动态增减，工作流层采用 Flowable 支持审批流程的灵活定制。前端路由采用动态加载机制，菜单和权限可由后台配置实时调整。

### 1.2 总体架构说明

系统整体采用前后端分离的双端架构，由以下几个核心部分组成：

- **后台管理端（yu-ui）**：基于 Vue2 + ElementUI 技术栈构建，面向教务管理人员提供全面的教务管理功能。包含学生管理、教师管理、课程管理、选课管理、成绩管理、排课管理、OA 审批等核心业务模块。
- **师生门户端（yu-portal-ui）**：基于轻量级 Vue2 构建，面向教师和学生提供自助服务功能。支持 PC 端和移动端双端访问，提供成绩查询、选课操作、课表查看、通知公告等功能。
- **后端服务（yu-admin）**：基于 Spring Boot 4.0.# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端 API 服务于多个前端入口（管理端、门户端、未来可能的移动 App 等），最大化接口复用率。

**模块化设计原则**：后端采用 Maven 多模块架构，将系统拆分为 13 个功能模块，每个模块承担明确的业务职责。模块之间通过依赖注入和接口抽象实现松耦合，遵循"高内聚、低耦合"的设计哲学。新增业务功能时只需创建新的业务模块，不影响已有模块的稳定性。

**分层架构原则**：在每个模块内部，严格遵循表现层（Controller）、业务层（Service）、持久层（Mapper）的三层分离模式。表现层负责协议处理和参数校验，业务层负责核心业务逻辑和事务管理，持久层负责数据访问。基础设施层（yu-framework）提供横切关注点的统一处理，包括安全认证、缓存管理、数据源切换、日志记录等。

**可扩展性原则**：系统在设计时充分考虑了未来业务增长的需要。数据层支持读写分离和多数据源切换，缓存层采用 Redis 提供高性能数据访问，调度层采用 Quartz 支持定时任务的动态增减，工作流层采用 Flowable 支持审批流程的灵活定制。前端路由采用动态加载机制，菜单和权限可由后台配置实时调整。

### 1.2 总体架构说明

系统整体采用前后端分离的双端架构，由以下几个核心部分组成：

- **后台管理端（yu-ui）**：基于 Vue2 + ElementUI 技术栈构建，面向教务管理人员提供全面的教务管理功能。包含学生管理、教师管理、课程管理、选课管理、成绩管理、排课管理、OA 审批等核心业务模块。
- **师生门户端（yu-portal-ui）**：基于轻量级 Vue2 构建，面向教师和学生提供自助服务功能。支持 PC 端和移动端双端访问，提供成绩查询、选课操作、课表查看、通知公告等功能。
- **后端服务（yu-admin）**：基于 Spring Boot 4.0.3 构建的多模块 Maven 工程，作为统一的 API 服务提供者，监听 80# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端 API 服务于多个前端入口（管理端、门户端、未来可能的移动 App 等），最大化接口复用率。

**模块化设计原则**：后端采用 Maven 多模块架构，将系统拆分为 13 个功能模块，每个模块承担明确的业务职责。模块之间通过依赖注入和接口抽象实现松耦合，遵循"高内聚、低耦合"的设计哲学。新增业务功能时只需创建新的业务模块，不影响已有模块的稳定性。

**分层架构原则**：在每个模块内部，严格遵循表现层（Controller）、业务层（Service）、持久层（Mapper）的三层分离模式。表现层负责协议处理和参数校验，业务层负责核心业务逻辑和事务管理，持久层负责数据访问。基础设施层（yu-framework）提供横切关注点的统一处理，包括安全认证、缓存管理、数据源切换、日志记录等。

**可扩展性原则**：系统在设计时充分考虑了未来业务增长的需要。数据层支持读写分离和多数据源切换，缓存层采用 Redis 提供高性能数据访问，调度层采用 Quartz 支持定时任务的动态增减，工作流层采用 Flowable 支持审批流程的灵活定制。前端路由采用动态加载机制，菜单和权限可由后台配置实时调整。

### 1.2 总体架构说明

系统整体采用前后端分离的双端架构，由以下几个核心部分组成：

- **后台管理端（yu-ui）**：基于 Vue2 + ElementUI 技术栈构建，面向教务管理人员提供全面的教务管理功能。包含学生管理、教师管理、课程管理、选课管理、成绩管理、排课管理、OA 审批等核心业务模块。
- **师生门户端（yu-portal-ui）**：基于轻量级 Vue2 构建，面向教师和学生提供自助服务功能。支持 PC 端和移动端双端访问，提供成绩查询、选课操作、课表查看、通知公告等功能。
- **后端服务（yu-admin）**：基于 Spring Boot 4.0.3 构建的多模块 Maven 工程，作为统一的 API 服务提供者，监听 8080 端口。整合了 Spring Security 认证、MyBatis 持久# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端 API 服务于多个前端入口（管理端、门户端、未来可能的移动 App 等），最大化接口复用率。

**模块化设计原则**：后端采用 Maven 多模块架构，将系统拆分为 13 个功能模块，每个模块承担明确的业务职责。模块之间通过依赖注入和接口抽象实现松耦合，遵循"高内聚、低耦合"的设计哲学。新增业务功能时只需创建新的业务模块，不影响已有模块的稳定性。

**分层架构原则**：在每个模块内部，严格遵循表现层（Controller）、业务层（Service）、持久层（Mapper）的三层分离模式。表现层负责协议处理和参数校验，业务层负责核心业务逻辑和事务管理，持久层负责数据访问。基础设施层（yu-framework）提供横切关注点的统一处理，包括安全认证、缓存管理、数据源切换、日志记录等。

**可扩展性原则**：系统在设计时充分考虑了未来业务增长的需要。数据层支持读写分离和多数据源切换，缓存层采用 Redis 提供高性能数据访问，调度层采用 Quartz 支持定时任务的动态增减，工作流层采用 Flowable 支持审批流程的灵活定制。前端路由采用动态加载机制，菜单和权限可由后台配置实时调整。

### 1.2 总体架构说明

系统整体采用前后端分离的双端架构，由以下几个核心部分组成：

- **后台管理端（yu-ui）**：基于 Vue2 + ElementUI 技术栈构建，面向教务管理人员提供全面的教务管理功能。包含学生管理、教师管理、课程管理、选课管理、成绩管理、排课管理、OA 审批等核心业务模块。
- **师生门户端（yu-portal-ui）**：基于轻量级 Vue2 构建，面向教师和学生提供自助服务功能。支持 PC 端和移动端双端访问，提供成绩查询、选课操作、课表查看、通知公告等功能。
- **后端服务（yu-admin）**：基于 Spring Boot 4.0.3 构建的多模块 Maven 工程，作为统一的 API 服务提供者，监听 8080 端口。整合了 Spring Security 认证、MyBatis 持久化、Redis 缓存、Quartz 调度、Flowable 工作流等基础设施。
- **数据层**：MySQL# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端 API 服务于多个前端入口（管理端、门户端、未来可能的移动 App 等），最大化接口复用率。

**模块化设计原则**：后端采用 Maven 多模块架构，将系统拆分为 13 个功能模块，每个模块承担明确的业务职责。模块之间通过依赖注入和接口抽象实现松耦合，遵循"高内聚、低耦合"的设计哲学。新增业务功能时只需创建新的业务模块，不影响已有模块的稳定性。

**分层架构原则**：在每个模块内部，严格遵循表现层（Controller）、业务层（Service）、持久层（Mapper）的三层分离模式。表现层负责协议处理和参数校验，业务层负责核心业务逻辑和事务管理，持久层负责数据访问。基础设施层（yu-framework）提供横切关注点的统一处理，包括安全认证、缓存管理、数据源切换、日志记录等。

**可扩展性原则**：系统在设计时充分考虑了未来业务增长的需要。数据层支持读写分离和多数据源切换，缓存层采用 Redis 提供高性能数据访问，调度层采用 Quartz 支持定时任务的动态增减，工作流层采用 Flowable 支持审批流程的灵活定制。前端路由采用动态加载机制，菜单和权限可由后台配置实时调整。

### 1.2 总体架构说明

系统整体采用前后端分离的双端架构，由以下几个核心部分组成：

- **后台管理端（yu-ui）**：基于 Vue2 + ElementUI 技术栈构建，面向教务管理人员提供全面的教务管理功能。包含学生管理、教师管理、课程管理、选课管理、成绩管理、排课管理、OA 审批等核心业务模块。
- **师生门户端（yu-portal-ui）**：基于轻量级 Vue2 构建，面向教师和学生提供自助服务功能。支持 PC 端和移动端双端访问，提供成绩查询、选课操作、课表查看、通知公告等功能。
- **后端服务（yu-admin）**：基于 Spring Boot 4.0.3 构建的多模块 Maven 工程，作为统一的 API 服务提供者，监听 8080 端口。整合了 Spring Security 认证、MyBatis 持久化、Redis 缓存、Quartz 调度、Flowable 工作流等基础设施。
- **数据层**：MySQL 8.x 作为主数据库（库名 yu-C# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端 API 服务于多个前端入口（管理端、门户端、未来可能的移动 App 等），最大化接口复用率。

**模块化设计原则**：后端采用 Maven 多模块架构，将系统拆分为 13 个功能模块，每个模块承担明确的业务职责。模块之间通过依赖注入和接口抽象实现松耦合，遵循"高内聚、低耦合"的设计哲学。新增业务功能时只需创建新的业务模块，不影响已有模块的稳定性。

**分层架构原则**：在每个模块内部，严格遵循表现层（Controller）、业务层（Service）、持久层（Mapper）的三层分离模式。表现层负责协议处理和参数校验，业务层负责核心业务逻辑和事务管理，持久层负责数据访问。基础设施层（yu-framework）提供横切关注点的统一处理，包括安全认证、缓存管理、数据源切换、日志记录等。

**可扩展性原则**：系统在设计时充分考虑了未来业务增长的需要。数据层支持读写分离和多数据源切换，缓存层采用 Redis 提供高性能数据访问，调度层采用 Quartz 支持定时任务的动态增减，工作流层采用 Flowable 支持审批流程的灵活定制。前端路由采用动态加载机制，菜单和权限可由后台配置实时调整。

### 1.2 总体架构说明

系统整体采用前后端分离的双端架构，由以下几个核心部分组成：

- **后台管理端（yu-ui）**：基于 Vue2 + ElementUI 技术栈构建，面向教务管理人员提供全面的教务管理功能。包含学生管理、教师管理、课程管理、选课管理、成绩管理、排课管理、OA 审批等核心业务模块。
- **师生门户端（yu-portal-ui）**：基于轻量级 Vue2 构建，面向教师和学生提供自助服务功能。支持 PC 端和移动端双端访问，提供成绩查询、选课操作、课表查看、通知公告等功能。
- **后端服务（yu-admin）**：基于 Spring Boot 4.0.3 构建的多模块 Maven 工程，作为统一的 API 服务提供者，监听 8080 端口。整合了 Spring Security 认证、MyBatis 持久化、Redis 缓存、Quartz 调度、Flowable 工作流等基础设施。
- **数据层**：MySQL 8.x 作为主数据库（库名 yu-CjluJwc），Redis 5.x 作为缓存和会话存储层，分别承担持久# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端 API 服务于多个前端入口（管理端、门户端、未来可能的移动 App 等），最大化接口复用率。

**模块化设计原则**：后端采用 Maven 多模块架构，将系统拆分为 13 个功能模块，每个模块承担明确的业务职责。模块之间通过依赖注入和接口抽象实现松耦合，遵循"高内聚、低耦合"的设计哲学。新增业务功能时只需创建新的业务模块，不影响已有模块的稳定性。

**分层架构原则**：在每个模块内部，严格遵循表现层（Controller）、业务层（Service）、持久层（Mapper）的三层分离模式。表现层负责协议处理和参数校验，业务层负责核心业务逻辑和事务管理，持久层负责数据访问。基础设施层（yu-framework）提供横切关注点的统一处理，包括安全认证、缓存管理、数据源切换、日志记录等。

**可扩展性原则**：系统在设计时充分考虑了未来业务增长的需要。数据层支持读写分离和多数据源切换，缓存层采用 Redis 提供高性能数据访问，调度层采用 Quartz 支持定时任务的动态增减，工作流层采用 Flowable 支持审批流程的灵活定制。前端路由采用动态加载机制，菜单和权限可由后台配置实时调整。

### 1.2 总体架构说明

系统整体采用前后端分离的双端架构，由以下几个核心部分组成：

- **后台管理端（yu-ui）**：基于 Vue2 + ElementUI 技术栈构建，面向教务管理人员提供全面的教务管理功能。包含学生管理、教师管理、课程管理、选课管理、成绩管理、排课管理、OA 审批等核心业务模块。
- **师生门户端（yu-portal-ui）**：基于轻量级 Vue2 构建，面向教师和学生提供自助服务功能。支持 PC 端和移动端双端访问，提供成绩查询、选课操作、课表查看、通知公告等功能。
- **后端服务（yu-admin）**：基于 Spring Boot 4.0.3 构建的多模块 Maven 工程，作为统一的 API 服务提供者，监听 8080 端口。整合了 Spring Security 认证、MyBatis 持久化、Redis 缓存、Quartz 调度、Flowable 工作流等基础设施。
- **数据层**：MySQL 8.x 作为主数据库（库名 yu-CjluJwc），Redis 5.x 作为缓存和会话存储层，分别承担持久化存储和高速缓存职责。
- **工作# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端 API 服务于多个前端入口（管理端、门户端、未来可能的移动 App 等），最大化接口复用率。

**模块化设计原则**：后端采用 Maven 多模块架构，将系统拆分为 13 个功能模块，每个模块承担明确的业务职责。模块之间通过依赖注入和接口抽象实现松耦合，遵循"高内聚、低耦合"的设计哲学。新增业务功能时只需创建新的业务模块，不影响已有模块的稳定性。

**分层架构原则**：在每个模块内部，严格遵循表现层（Controller）、业务层（Service）、持久层（Mapper）的三层分离模式。表现层负责协议处理和参数校验，业务层负责核心业务逻辑和事务管理，持久层负责数据访问。基础设施层（yu-framework）提供横切关注点的统一处理，包括安全认证、缓存管理、数据源切换、日志记录等。

**可扩展性原则**：系统在设计时充分考虑了未来业务增长的需要。数据层支持读写分离和多数据源切换，缓存层采用 Redis 提供高性能数据访问，调度层采用 Quartz 支持定时任务的动态增减，工作流层采用 Flowable 支持审批流程的灵活定制。前端路由采用动态加载机制，菜单和权限可由后台配置实时调整。

### 1.2 总体架构说明

系统整体采用前后端分离的双端架构，由以下几个核心部分组成：

- **后台管理端（yu-ui）**：基于 Vue2 + ElementUI 技术栈构建，面向教务管理人员提供全面的教务管理功能。包含学生管理、教师管理、课程管理、选课管理、成绩管理、排课管理、OA 审批等核心业务模块。
- **师生门户端（yu-portal-ui）**：基于轻量级 Vue2 构建，面向教师和学生提供自助服务功能。支持 PC 端和移动端双端访问，提供成绩查询、选课操作、课表查看、通知公告等功能。
- **后端服务（yu-admin）**：基于 Spring Boot 4.0.3 构建的多模块 Maven 工程，作为统一的 API 服务提供者，监听 8080 端口。整合了 Spring Security 认证、MyBatis 持久化、Redis 缓存、Quartz 调度、Flowable 工作流等基础设施。
- **数据层**：MySQL 8.x 作为主数据库（库名 yu-CjluJwc），Redis 5.x 作为缓存和会话存储层，分别承担持久化存储和高速缓存职责。
- **工作流引擎**：Flowable 7.1.0 用于 OA 审批流程的建模、执行和监控，支持请假审批、公文# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端 API 服务于多个前端入口（管理端、门户端、未来可能的移动 App 等），最大化接口复用率。

**模块化设计原则**：后端采用 Maven 多模块架构，将系统拆分为 13 个功能模块，每个模块承担明确的业务职责。模块之间通过依赖注入和接口抽象实现松耦合，遵循"高内聚、低耦合"的设计哲学。新增业务功能时只需创建新的业务模块，不影响已有模块的稳定性。

**分层架构原则**：在每个模块内部，严格遵循表现层（Controller）、业务层（Service）、持久层（Mapper）的三层分离模式。表现层负责协议处理和参数校验，业务层负责核心业务逻辑和事务管理，持久层负责数据访问。基础设施层（yu-framework）提供横切关注点的统一处理，包括安全认证、缓存管理、数据源切换、日志记录等。

**可扩展性原则**：系统在设计时充分考虑了未来业务增长的需要。数据层支持读写分离和多数据源切换，缓存层采用 Redis 提供高性能数据访问，调度层采用 Quartz 支持定时任务的动态增减，工作流层采用 Flowable 支持审批流程的灵活定制。前端路由采用动态加载机制，菜单和权限可由后台配置实时调整。

### 1.2 总体架构说明

系统整体采用前后端分离的双端架构，由以下几个核心部分组成：

- **后台管理端（yu-ui）**：基于 Vue2 + ElementUI 技术栈构建，面向教务管理人员提供全面的教务管理功能。包含学生管理、教师管理、课程管理、选课管理、成绩管理、排课管理、OA 审批等核心业务模块。
- **师生门户端（yu-portal-ui）**：基于轻量级 Vue2 构建，面向教师和学生提供自助服务功能。支持 PC 端和移动端双端访问，提供成绩查询、选课操作、课表查看、通知公告等功能。
- **后端服务（yu-admin）**：基于 Spring Boot 4.0.3 构建的多模块 Maven 工程，作为统一的 API 服务提供者，监听 8080 端口。整合了 Spring Security 认证、MyBatis 持久化、Redis 缓存、Quartz 调度、Flowable 工作流等基础设施。
- **数据层**：MySQL 8.x 作为主数据库（库名 yu-CjluJwc），Redis 5.x 作为缓存和会话存储层，分别承担持久化存储和高速缓存职责。
- **工作流引擎**：Flowable 7.1.0 用于 OA 审批流程的建模、执行和监控，支持请假审批、公文流转等业务场景。
- **调度引擎**：Quartz 负责定时任务调度，# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端 API 服务于多个前端入口（管理端、门户端、未来可能的移动 App 等），最大化接口复用率。

**模块化设计原则**：后端采用 Maven 多模块架构，将系统拆分为 13 个功能模块，每个模块承担明确的业务职责。模块之间通过依赖注入和接口抽象实现松耦合，遵循"高内聚、低耦合"的设计哲学。新增业务功能时只需创建新的业务模块，不影响已有模块的稳定性。

**分层架构原则**：在每个模块内部，严格遵循表现层（Controller）、业务层（Service）、持久层（Mapper）的三层分离模式。表现层负责协议处理和参数校验，业务层负责核心业务逻辑和事务管理，持久层负责数据访问。基础设施层（yu-framework）提供横切关注点的统一处理，包括安全认证、缓存管理、数据源切换、日志记录等。

**可扩展性原则**：系统在设计时充分考虑了未来业务增长的需要。数据层支持读写分离和多数据源切换，缓存层采用 Redis 提供高性能数据访问，调度层采用 Quartz 支持定时任务的动态增减，工作流层采用 Flowable 支持审批流程的灵活定制。前端路由采用动态加载机制，菜单和权限可由后台配置实时调整。

### 1.2 总体架构说明

系统整体采用前后端分离的双端架构，由以下几个核心部分组成：

- **后台管理端（yu-ui）**：基于 Vue2 + ElementUI 技术栈构建，面向教务管理人员提供全面的教务管理功能。包含学生管理、教师管理、课程管理、选课管理、成绩管理、排课管理、OA 审批等核心业务模块。
- **师生门户端（yu-portal-ui）**：基于轻量级 Vue2 构建，面向教师和学生提供自助服务功能。支持 PC 端和移动端双端访问，提供成绩查询、选课操作、课表查看、通知公告等功能。
- **后端服务（yu-admin）**：基于 Spring Boot 4.0.3 构建的多模块 Maven 工程，作为统一的 API 服务提供者，监听 8080 端口。整合了 Spring Security 认证、MyBatis 持久化、Redis 缓存、Quartz 调度、Flowable 工作流等基础设施。
- **数据层**：MySQL 8.x 作为主数据库（库名 yu-CjluJwc），Redis 5.x 作为缓存和会话存储层，分别承担持久化存储和高速缓存职责。
- **工作流引擎**：Flowable 7.1.0 用于 OA 审批流程的建模、执行和监控，支持请假审批、公文流转等业务场景。
- **调度引擎**：Quartz 负责定时任务调度，包括学业预警计算、数据同步、统计报表生成等周期性任务。

```
┌────────# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端 API 服务于多个前端入口（管理端、门户端、未来可能的移动 App 等），最大化接口复用率。

**模块化设计原则**：后端采用 Maven 多模块架构，将系统拆分为 13 个功能模块，每个模块承担明确的业务职责。模块之间通过依赖注入和接口抽象实现松耦合，遵循"高内聚、低耦合"的设计哲学。新增业务功能时只需创建新的业务模块，不影响已有模块的稳定性。

**分层架构原则**：在每个模块内部，严格遵循表现层（Controller）、业务层（Service）、持久层（Mapper）的三层分离模式。表现层负责协议处理和参数校验，业务层负责核心业务逻辑和事务管理，持久层负责数据访问。基础设施层（yu-framework）提供横切关注点的统一处理，包括安全认证、缓存管理、数据源切换、日志记录等。

**可扩展性原则**：系统在设计时充分考虑了未来业务增长的需要。数据层支持读写分离和多数据源切换，缓存层采用 Redis 提供高性能数据访问，调度层采用 Quartz 支持定时任务的动态增减，工作流层采用 Flowable 支持审批流程的灵活定制。前端路由采用动态加载机制，菜单和权限可由后台配置实时调整。

### 1.2 总体架构说明

系统整体采用前后端分离的双端架构，由以下几个核心部分组成：

- **后台管理端（yu-ui）**：基于 Vue2 + ElementUI 技术栈构建，面向教务管理人员提供全面的教务管理功能。包含学生管理、教师管理、课程管理、选课管理、成绩管理、排课管理、OA 审批等核心业务模块。
- **师生门户端（yu-portal-ui）**：基于轻量级 Vue2 构建，面向教师和学生提供自助服务功能。支持 PC 端和移动端双端访问，提供成绩查询、选课操作、课表查看、通知公告等功能。
- **后端服务（yu-admin）**：基于 Spring Boot 4.0.3 构建的多模块 Maven 工程，作为统一的 API 服务提供者，监听 8080 端口。整合了 Spring Security 认证、MyBatis 持久化、Redis 缓存、Quartz 调度、Flowable 工作流等基础设施。
- **数据层**：MySQL 8.x 作为主数据库（库名 yu-CjluJwc），Redis 5.x 作为缓存和会话存储层，分别承担持久化存储和高速缓存职责。
- **工作流引擎**：Flowable 7.1.0 用于 OA 审批流程的建模、执行和监控，支持请假审批、公文流转等业务场景。
- **调度引擎**：Quartz 负责定时任务调度，包括学业预警计算、数据同步、统计报表生成等周期性任务。

```
┌─────────────────────────────────────────────────────────────┐
│                      客户端层（浏览器/# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端 API 服务于多个前端入口（管理端、门户端、未来可能的移动 App 等），最大化接口复用率。

**模块化设计原则**：后端采用 Maven 多模块架构，将系统拆分为 13 个功能模块，每个模块承担明确的业务职责。模块之间通过依赖注入和接口抽象实现松耦合，遵循"高内聚、低耦合"的设计哲学。新增业务功能时只需创建新的业务模块，不影响已有模块的稳定性。

**分层架构原则**：在每个模块内部，严格遵循表现层（Controller）、业务层（Service）、持久层（Mapper）的三层分离模式。表现层负责协议处理和参数校验，业务层负责核心业务逻辑和事务管理，持久层负责数据访问。基础设施层（yu-framework）提供横切关注点的统一处理，包括安全认证、缓存管理、数据源切换、日志记录等。

**可扩展性原则**：系统在设计时充分考虑了未来业务增长的需要。数据层支持读写分离和多数据源切换，缓存层采用 Redis 提供高性能数据访问，调度层采用 Quartz 支持定时任务的动态增减，工作流层采用 Flowable 支持审批流程的灵活定制。前端路由采用动态加载机制，菜单和权限可由后台配置实时调整。

### 1.2 总体架构说明

系统整体采用前后端分离的双端架构，由以下几个核心部分组成：

- **后台管理端（yu-ui）**：基于 Vue2 + ElementUI 技术栈构建，面向教务管理人员提供全面的教务管理功能。包含学生管理、教师管理、课程管理、选课管理、成绩管理、排课管理、OA 审批等核心业务模块。
- **师生门户端（yu-portal-ui）**：基于轻量级 Vue2 构建，面向教师和学生提供自助服务功能。支持 PC 端和移动端双端访问，提供成绩查询、选课操作、课表查看、通知公告等功能。
- **后端服务（yu-admin）**：基于 Spring Boot 4.0.3 构建的多模块 Maven 工程，作为统一的 API 服务提供者，监听 8080 端口。整合了 Spring Security 认证、MyBatis 持久化、Redis 缓存、Quartz 调度、Flowable 工作流等基础设施。
- **数据层**：MySQL 8.x 作为主数据库（库名 yu-CjluJwc），Redis 5.x 作为缓存和会话存储层，分别承担持久化存储和高速缓存职责。
- **工作流引擎**：Flowable 7.1.0 用于 OA 审批流程的建模、执行和监控，支持请假审批、公文流转等业务场景。
- **调度引擎**：Quartz 负责定时任务调度，包括学业预警计算、数据同步、统计报表生成等周期性任务。

```
┌─────────────────────────────────────────────────────────────┐
│                      客户端层（浏览器/移动端）                  │
│  ┌──────────────────┐        ┌────────────────────────# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端 API 服务于多个前端入口（管理端、门户端、未来可能的移动 App 等），最大化接口复用率。

**模块化设计原则**：后端采用 Maven 多模块架构，将系统拆分为 13 个功能模块，每个模块承担明确的业务职责。模块之间通过依赖注入和接口抽象实现松耦合，遵循"高内聚、低耦合"的设计哲学。新增业务功能时只需创建新的业务模块，不影响已有模块的稳定性。

**分层架构原则**：在每个模块内部，严格遵循表现层（Controller）、业务层（Service）、持久层（Mapper）的三层分离模式。表现层负责协议处理和参数校验，业务层负责核心业务逻辑和事务管理，持久层负责数据访问。基础设施层（yu-framework）提供横切关注点的统一处理，包括安全认证、缓存管理、数据源切换、日志记录等。

**可扩展性原则**：系统在设计时充分考虑了未来业务增长的需要。数据层支持读写分离和多数据源切换，缓存层采用 Redis 提供高性能数据访问，调度层采用 Quartz 支持定时任务的动态增减，工作流层采用 Flowable 支持审批流程的灵活定制。前端路由采用动态加载机制，菜单和权限可由后台配置实时调整。

### 1.2 总体架构说明

系统整体采用前后端分离的双端架构，由以下几个核心部分组成：

- **后台管理端（yu-ui）**：基于 Vue2 + ElementUI 技术栈构建，面向教务管理人员提供全面的教务管理功能。包含学生管理、教师管理、课程管理、选课管理、成绩管理、排课管理、OA 审批等核心业务模块。
- **师生门户端（yu-portal-ui）**：基于轻量级 Vue2 构建，面向教师和学生提供自助服务功能。支持 PC 端和移动端双端访问，提供成绩查询、选课操作、课表查看、通知公告等功能。
- **后端服务（yu-admin）**：基于 Spring Boot 4.0.3 构建的多模块 Maven 工程，作为统一的 API 服务提供者，监听 8080 端口。整合了 Spring Security 认证、MyBatis 持久化、Redis 缓存、Quartz 调度、Flowable 工作流等基础设施。
- **数据层**：MySQL 8.x 作为主数据库（库名 yu-CjluJwc），Redis 5.x 作为缓存和会话存储层，分别承担持久化存储和高速缓存职责。
- **工作流引擎**：Flowable 7.1.0 用于 OA 审批流程的建模、执行和监控，支持请假审批、公文流转等业务场景。
- **调度引擎**：Quartz 负责定时任务调度，包括学业预警计算、数据同步、统计报表生成等周期性任务。

```
┌─────────────────────────────────────────────────────────────┐
│                      客户端层（浏览器/移动端）                  │
│  ┌──────────────────┐        ┌──────────────────────────┐   │
│  │  后台管理端 yu-ui  │        │# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端 API 服务于多个前端入口（管理端、门户端、未来可能的移动 App 等），最大化接口复用率。

**模块化设计原则**：后端采用 Maven 多模块架构，将系统拆分为 13 个功能模块，每个模块承担明确的业务职责。模块之间通过依赖注入和接口抽象实现松耦合，遵循"高内聚、低耦合"的设计哲学。新增业务功能时只需创建新的业务模块，不影响已有模块的稳定性。

**分层架构原则**：在每个模块内部，严格遵循表现层（Controller）、业务层（Service）、持久层（Mapper）的三层分离模式。表现层负责协议处理和参数校验，业务层负责核心业务逻辑和事务管理，持久层负责数据访问。基础设施层（yu-framework）提供横切关注点的统一处理，包括安全认证、缓存管理、数据源切换、日志记录等。

**可扩展性原则**：系统在设计时充分考虑了未来业务增长的需要。数据层支持读写分离和多数据源切换，缓存层采用 Redis 提供高性能数据访问，调度层采用 Quartz 支持定时任务的动态增减，工作流层采用 Flowable 支持审批流程的灵活定制。前端路由采用动态加载机制，菜单和权限可由后台配置实时调整。

### 1.2 总体架构说明

系统整体采用前后端分离的双端架构，由以下几个核心部分组成：

- **后台管理端（yu-ui）**：基于 Vue2 + ElementUI 技术栈构建，面向教务管理人员提供全面的教务管理功能。包含学生管理、教师管理、课程管理、选课管理、成绩管理、排课管理、OA 审批等核心业务模块。
- **师生门户端（yu-portal-ui）**：基于轻量级 Vue2 构建，面向教师和学生提供自助服务功能。支持 PC 端和移动端双端访问，提供成绩查询、选课操作、课表查看、通知公告等功能。
- **后端服务（yu-admin）**：基于 Spring Boot 4.0.3 构建的多模块 Maven 工程，作为统一的 API 服务提供者，监听 8080 端口。整合了 Spring Security 认证、MyBatis 持久化、Redis 缓存、Quartz 调度、Flowable 工作流等基础设施。
- **数据层**：MySQL 8.x 作为主数据库（库名 yu-CjluJwc），Redis 5.x 作为缓存和会话存储层，分别承担持久化存储和高速缓存职责。
- **工作流引擎**：Flowable 7.1.0 用于 OA 审批流程的建模、执行和监控，支持请假审批、公文流转等业务场景。
- **调度引擎**：Quartz 负责定时任务调度，包括学业预警计算、数据同步、统计报表生成等周期性任务。

```
┌─────────────────────────────────────────────────────────────┐
│                      客户端层（浏览器/移动端）                  │
│  ┌──────────────────┐        ┌──────────────────────────┐   │
│  │  后台管理端 yu-ui  │        │  师生门户端 yu-portal-ui  │   │
│  │  Vue2+ElementUI   │        │  Vue2（PC+移动端）         │# 系统架构设计

> **项目名称**：长江大学教务处综合管理系统（CjluJwc）
> **文档版本**：V1.0
> **编写日期**：2026-08-04

---

## 目录

1. [架构概述](#1-架构概述)
2. [技术架构](#2-技术架构)
3. [后端模块设计](#3-后端模块设计)
4. [安全架构](#4-安全架构)
5. [前端架构](#5-前端架构)
6. [基础设施设计](#6-基础设施设计)
7. [部署架构](#7-部署架构)

---

## 1. 架构概述

### 1.1 设计原则

长江大学教务处综合管理系统的架构设计遵循以下核心原则，确保系统在功能性、可维护性、可扩展性和安全性等方面达到高校教务管理的高标准要求。

**前后端分离原则**：系统采用严格的前后端分离架构，后端提供统一的 RESTful API 接口，前端通过 HTTP/HTTPS 协议消费接口数据。这种分离使得前后端团队可以独立开发、独立部署、独立演进，同时允许同一套后端 API 服务于多个前端入口（管理端、门户端、未来可能的移动 App 等），最大化接口复用率。

**模块化设计原则**：后端采用 Maven 多模块架构，将系统拆分为 13 个功能模块，每个模块承担明确的业务职责。模块之间通过依赖注入和接口抽象实现松耦合，遵循"高内聚、低耦合"的设计哲学。新增业务功能时只需创建新的业务模块，不影响已有模块的稳定性。

**分层架构原则**：在每个模块内部，严格遵循表现层（Controller）、业务层（Service）、持久层（Mapper）的三层分离模式。表现层负责协议处理和参数校验，业务层负责核心业务逻辑和事务管理，持久层负责数据访问。基础设施层（yu-framework）提供横切关注点的统一处理，包括安全认证、缓存管理、数据源切换、日志记录等。

**可扩展性原则**：系统在设计时充分考虑了未来业务增长的需要。数据层支持读写分离和多数据源切换，缓存层采用 Redis 提供高性能数据访问，调度层采用 Quartz 支持定时任务的动态增减，工作流层采用 Flowable 支持审批流程的灵活定制。前端路由采用动态加载机制，菜单和权限可由后台配置实时调整。

### 1.2 总体架构说明

系统整体采用前后端分离的双端架构，由以下几个核心部分组成：

- **后台管理端（yu-ui）**：基于 Vue2 + ElementUI 技术栈构建，面向教务管理人员提供全面的教务管理功能。包含学生管理、教师管理、课程管理、选课管理、成绩管理、排课管理、OA 审批等核心业务模块。
- **师生门户端（yu-portal-ui）**：基于轻量级 Vue2 构建，面向教师和学生提供自助服务功能。支持 PC 端和移动端双端访问，提供成绩查询、选课操作、课表查看、通知公告等功能。
- **后端服务（yu-admin）**：基于 Spring Boot 4.0.3 构建的多模块 Maven 工程，作为统一的 API 服务提供者，监听 8080 端口。整合了 Spring Security 认证、MyBatis 持久化、Redis 缓存、Quartz 调度、Flowable 工作流等基础设施。
- **数据层**：MySQL 8.x 作为主数据库（库名 yu-CjluJwc），Redis 5.x 作为缓存和会话存储层，分别承担持久化存储和高速缓存职责。
- **工作流引擎**：Flowable 7.1.0 用于 OA 审批流程的建模、执行和监控，支持请假审批、公文流转等业务场景。
- **调度引擎**：Quartz 负责定时任务调度，包括学业预警计算、数据同步、统计报表生成等周期性任务。

```
┌─────────────────────────────────────────────────────────────┐
│                      客户端层（浏览器/移动端）                  │
│  ┌──────────────────┐        ┌──────────────────────────┐   │
│  │  后台管理端 yu-ui  │        │  师生门户端 yu-portal-ui  │   │
│  │  Vue2+ElementUI   │        │  Vue2（PC+移动端）         │   │
│  └────────┬─────────┘        └───────────┬────────
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yu.aem.mapper.AemGradeRecordMapper">
    <resultMap type="AemGradeRecord" id="AemGradeRecordResult">
        <result property="gradeId"        column="grade_id"         />
        <result property="studentId"      column="student_id"       />
        <result property="courseId"       column="course_id"        />
        <result property="semesterId"     column="semester_id"      />
        <result property="examType"       column="exam_type"        />
        <result property="regularScore"   column="regular_score"    />
        <result property="examScore"      column="exam_score"       />
        <result property="totalScore"     column="total_score"      />
        <result property="gradePoint"     column="grade_point"      />
        <result property="gradeLevel"     column="grade_level"      />
        <result property="isPass"         column="is_pass"          />
        <result property="isReviewed"     column="is_reviewed"      />
        <result property="status"         column="status"           />
        <result property="createBy"       column="create_by"        />
        <result property="createTime"     column="create_time"      />
        <result property="updateBy"       column="update_by"        />
        <result property="updateTime"     column="update_time"      />
        <result property="remark"         column="remark"           />
        <result property="credit"         column="credit"           />
    </resultMap>
    <sql id="selectAemGradeRecordVo">
        select grade_id, student_id, course_id, semester_id, exam_type, regular_score, exam_score, total_score, grade_point, grade_level, is_pass, is_reviewed, status, create_by, create_time, update_by, update_time, remark from aem_grade_record
    </sql>
    <select id="selectAemGradeRecordList" parameterType="AemGradeRecord" resultMap="AemGradeRecordResult">
        select gr.grade_id, gr.student_id, gr.course_id, gr.semester_id, gr.exam_type, gr.regular_score, gr.exam_score, gr.total_score, gr.grade_point, gr.grade_level, gr.is_pass, gr.is_reviewed, gr.status, gr.create_by, gr.create_time, gr.update_by, gr.update_time, gr.remark
        from aem_grade_record gr
        left join sam_student ss on gr.student_id = ss.student_id
        left join sys_dept d on ss.dept_id = d.dept_id
        <where>
            <if test="studentId != null"> and gr.student_id = #{studentId}</if>
            <if test="courseId != null"> and gr.course_id = #{courseId}</if>
            <if test="semesterId != null"> and gr.semester_id = #{semesterId}</if>
            <if test="examType != null and examType.trim() != ''"> and gr.exam_type = #{examType}</if>
            <if test="gradeLevel != null and gradeLevel.trim() != ''"> and gr.grade_level = #{gradeLevel}</if>
            <if test="isPass != null and isPass.trim() != ''"> and gr.is_pass = #{isPass}</if>
            <if test="isReviewed != null and isReviewed.trim() != ''"> and gr.is_reviewed = #{isReviewed}</if>
            <if test="status != null and status.trim() != ''"> and gr.status = #{status}</if>
            <!-- 数据范围过滤 -->
            ${params.dataScope}
        </where>
    </select>
    <select id="selectAemGradeRecordByGradeId" parameterType="Long" resultMap="AemGradeRecordResult">
        <include refid="selectAemGradeRecordVo"/> where grade_id = #{gradeId}
    </select>
    <insert id="insertAemGradeRecord" parameterType="AemGradeRecord" useGeneratedKeys="true" keyProperty="gradeId">
        insert into aem_grade_record
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="studentId != null">student_id,</if>
            <if test="courseId != null">course_id,</if>
            <if test="semesterId != null">semester_id,</if>
            <if test="examType != null">exam_type,</if>
            <if test="regularScore != null">regular_score,</if>
            <if test="examScore != null">exam_score,</if>
            <if test="totalScore != null">total_score,</if>
            <if test="gradePoint != null">grade_point,</if>
            <if test="gradeLevel != null">grade_level,</if>
            <if test="isPass != null">is_pass,</if>
            <if test="isReviewed != null">is_reviewed,</if>
            <if test="status != null">status,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="remark != null">remark,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="studentId != null">#{studentId},</if>
            <if test="courseId != null">#{courseId},</if>
            <if test="semesterId != null">#{semesterId},</if>
            <if test="examType != null">#{examType},</if>
            <if test="regularScore != null">#{regularScore},</if>
            <if test="examScore != null">#{examScore},</if>
            <if test="totalScore != null">#{totalScore},</if>
            <if test="gradePoint != null">#{gradePoint},</if>
            <if test="gradeLevel != null">#{gradeLevel},</if>
            <if test="isPass != null">#{isPass},</if>
            <if test="isReviewed != null">#{isReviewed},</if>
            <if test="status != null">#{status},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="remark != null">#{remark},</if>
        </trim>
    </insert>
    <update id="updateAemGradeRecord" parameterType="AemGradeRecord">
        update aem_grade_record
        <trim prefix="SET" suffixOverrides=",">
            <if test="studentId != null">student_id = #{studentId},</if>
            <if test="courseId != null">course_id = #{courseId},</if>
            <if test="semesterId != null">semester_id = #{semesterId},</if>
            <if test="examType != null">exam_type = #{examType},</if>
            <if test="regularScore != null">regular_score = #{regularScore},</if>
            <if test="examScore != null">exam_score = #{examScore},</if>
            <if test="totalScore != null">total_score = #{totalScore},</if>
            <if test="gradePoint != null">grade_point = #{gradePoint},</if>
            <if test="gradeLevel != null">grade_level = #{gradeLevel},</if>
            <if test="isPass != null">is_pass = #{isPass},</if>
            <if test="isReviewed != null">is_reviewed = #{isReviewed},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="remark != null">remark = #{remark},</if>
        </trim>
        where grade_id = #{gradeId}
    </update>
    <delete id="deleteAemGradeRecordByGradeId" parameterType="Long">
        delete from aem_grade_record where grade_id = #{gradeId}
    </delete>
    <delete id="deleteAemGradeRecordByGradeIds" parameterType="String">
        delete from aem_grade_record where grade_id in
        <foreach item="gradeId" collection="array" open="(" separator="," close=")">#{gradeId}</foreach>
    </delete>
    <select id="selectByStudentAndSemester" resultMap="AemGradeRecordResult">
        select gr.grade_id, gr.student_id, gr.course_id, gr.semester_id, gr.exam_type,
               gr.regular_score, gr.exam_score, gr.total_score, gr.grade_point, gr.grade_level,
               gr.is_pass, gr.is_reviewed, gr.status, gr.create_by, gr.create_time,
               gr.update_by, gr.update_time, gr.remark, cl.credit as credit
        from aem_grade_record gr
        left join tpm_course_library cl on gr.course_id = cl.course_id
        where gr.student_id = #{studentId}
        <if test="semesterId != null"> and gr.semester_id = #{semesterId}</if>
    </select>
    <select id="selectBySemester" parameterType="Long" resultMap="AemGradeRecordResult">
        select gr.grade_id, gr.student_id, gr.course_id, gr.semester_id, gr.exam_type,
               gr.regular_score, gr.exam_score, gr.total_score, gr.grade_point, gr.grade_level,
               gr.is_pass, gr.is_reviewed, gr.status, gr.create_by, gr.create_time,
               gr.update_by, gr.update_time, gr.remark, cl.credit as credit
        from aem_grade_record gr
        left join tpm_course_library cl on gr.course_id = cl.course_id
        where gr.semester_id = #{semesterId}
    </select>
    <update id="updateGradePointBatch">
        update aem_grade_record
        set grade_point = case grade_id
        <foreach collection="list" item="item">
            when #{item.gradeId} then #{item.gradePoint}
        </foreach>
        end,
        grade_level = case grade_id
        <foreach collection="list" item="item">
            when #{item.gradeId} then #{item.gradeLevel}
        </foreach>
        end
        where grade_id in
        <foreach collection="list" item="item" open="(" separator="," close=")">#{item.gradeId}</foreach>
    </update>
    <select id="selectCourseCreditByCourseId" parameterType="Long" resultType="java.lang.Double">
        select credit from tpm_course_library where course_id = #{courseId}
    </select>

    <!-- 批量新增成绩（Excel导入），重复键(uk_grade_scset)时更新分数字段 -->
    <insert id="batchInsert" parameterType="java.util.List">
        insert into aem_grade_record
        (student_id, course_id, semester_id, exam_type, regular_score, exam_score,
         total_score, grade_point, grade_level, is_pass, is_reviewed, status, create_by, create_time)
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.studentId}, #{item.courseId}, #{item.semesterId}, #{item.examType},
             #{item.regularScore}, #{item.examScore}, #{item.totalScore}, #{item.gradePoint},
             #{item.gradeLevel}, #{item.isPass}, #{item.isReviewed}, #{item.status},
             #{item.createBy}, #{item.createTime})
        </foreach>
        on duplicate key update
            regular_score = values(regular_score),
            exam_score = values(exam_score),
            total_score = values(total_score),
            grade_point = values(grade_point),
            grade_level = values(grade_level),
            is_pass = values(is_pass),
            update_by = values(create_by),
            update_time = now()
    </insert>
</mapper>

<?xml version="1.0" encoding="UTF-8"?>
<definitions xmlns="http://www.omg.org/spec/BPMN/20100524/MODEL"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xmlns:flowable="http://flowable.org/bpmn"
             targetNamespace="http://www.flowable.org/processdef">

    <process id="oa-document-flow" name="公文审批流程" isExecutable="true">
        <startEvent id="start" name="开始"/>
        <sequenceFlow id="flow1" sourceRef="start" targetRef="dept-leader-approval"/>

        <userTask id="dept-leader-approval" name="部门领导审批" flowable:assignee="${deptLeader}"/>
        <sequenceFlow id="flow2" sourceRef="dept-leader-approval" targetRef="approve-gateway"/>

        <exclusiveGateway id="approve-gateway" name="审批网关"/>
        <sequenceFlow id="flow3" sourceRef="approve-gateway" targetRef="office-approval">
            <conditionExpression xsi:type="tFormalExpression">${approved == true}</conditionExpression>
        </sequenceFlow>
        <sequenceFlow id="flow4" sourceRef="approve-gateway" targetRef="end-rejected">
            <conditionExpression xsi:type="tFormalExpression">${approved == false}</conditionExpression>
        </sequenceFlow>

        <userTask id="office-approval" name="办公室复核" flowable:assignee="${officeApprover}"/>
        <sequenceFlow id="flow5" sourceRef="office-approval" targetRef="publish-task"/>

        <userTask id="publish-task" name="发布公文" flowable:assignee="${publisher}"/>
        <sequenceFlow id="flow6" sourceRef="publish-task" targetRef="end-approved"/>

        <endEvent id="end-approved" name="审批通过"/>
        <endEvent id="end-rejected" name="审批驳回"/>
    </process>
</definitions>
